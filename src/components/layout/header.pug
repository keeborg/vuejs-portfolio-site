header.header
  .header__logo
    .logo ИК
  .header__nav
    nav.menu
      - var items = [['Главная', 'main'], ['Обо мне', 'about'], ['Навыки', 'skills'], ['Работы', 'portfolio'], ['Отзывы', 'reviews'], ['Связаться', 'contact']];
      ul.menu__list
        each item in items
          li.menu__item
            a(href="#"+item[1]).menu__link #{item[0]}
  .header__socials
    - var socials = ['insta', 'vk', 'github'];
    ul.socials
      each social in socials
        li.socials__item
          a(href="#").socials__link
            +icon(social, 'socials__icon')