.header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 65px 0;
}

.header__nav {
    margin: 0 20px;
}

.header__logo {
    .logo {
        font-size: 36px;
        font-weight: 800;
    }
}